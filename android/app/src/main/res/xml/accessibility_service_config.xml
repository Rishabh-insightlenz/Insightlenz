<?xml version="1.0" encoding="utf-8"?>
<!--
    Accessibility service configuration for AppLaunchDetector.

    We only request TYPE_WINDOW_STATE_CHANGED — fires when the user switches apps.
    We do NOT request canRetrieveWindowContent — we don't read screen content,
    only package names. This keeps the permission scope minimal and honest.
-->
<accessibility-service
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagReportViewIds"
    android:canRetrieveWindowContent="false"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100"
    android:settingsActivity=".MainActivity" />
